import { StyleSheet } from "react-native";

import { Colors } from "../../utils/Colors";

export const screenMainStyle = StyleSheet.create({
  main: {
    flex: 1,
    paddingHorizontal: 27,
    paddingTop: 65,
    backgroundColor: Colors.WHITE,
  },
});